// TODO: Implement menu routes
// Example: GET /v1/menu/items, POST /v1/menu/items, GET /v1/menu/categories

import { Router } from "express";

export const menuRouter = Router();

// menuRouter.get('/v1/menu/items', ...);
// menuRouter.post('/v1/menu/items', ...);
// menuRouter.get('/v1/menu/categories', ...);
